/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // images: {
    //     loader: 'custom',
    //     loaderFile: './myloader.ts',
    //     // unoptimized: true
    // }
}

module.exports = nextConfig
